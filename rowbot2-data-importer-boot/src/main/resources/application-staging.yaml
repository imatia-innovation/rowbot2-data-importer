environment:
  profile: staging
logging:
  level:
    com.imatia: INFO
    org.springframework.boot.autoconfigure: INFO
management:
  security:
    enabled: false
  server:
    base-path: /
    port: 8081
    servlet:
      context-path: /
  endpoints:
    web:
      base-path: /management
